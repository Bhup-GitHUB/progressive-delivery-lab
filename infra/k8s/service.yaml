apiVersion: v1
kind: Service
metadata:
  name: checkout
spec:
  selector:
    app: checkout
  ports:
    - name: http
      port: 8080
      targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: flag-service
spec:
  selector:
    app: flag-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
